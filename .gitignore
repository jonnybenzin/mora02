# Secrets
.env
.env.*
!.env.example

# Security scan reports (may contain Klartext-secrets)
gitleaks-report.json
trufflehog-report.json

# Scripts Archive (enthält env-centralize.sh mit Klartext-Secrets)
scripts/_archive/

# Zentrales Archive für reversibles Aufräumen (ADR-021)
# Inhalt ist lokal-only; Borg-Backup deckt die Sicherheits-Schicht ab
_archive/

# Sensitive lokale Konfig (VPN-Keys, künftige Secret-Files) — KRITISCH
secrets/

# Run-Time State von Host-Tools (xray-Reports etc.)
state/

# Claude Code Sessions + User-Input — privat
claude/

# Pilot Run-Time-Daten (chathistory JSONs mit Konversations-Inhalten)
apps/pilot/data/

# Claude Code & MCP runtime data
.claude/
.playwright-mcp/

# Generated / large app data (keep out of repo)
apps/batch-imagegen/
apps/blender-worker/

# Laufzeit-Daten
volumes/
backups/
output/

# Penpot Assets (massenhaft generierte Dateien)
docker/penpot/

# Python
__pycache__/
*.pyc
venv/
venv_rag/
scripts/venv/

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# Generierte Bilder / Factory Output
kompendium/
ai_models/
ai-models/
docker/ComfyUI/output/
docker/ComfyUI/input/
docker/ComfyUI/models/
nginx-images/

# Grosse Modell-Dateien
*.ckpt
*.safetensors
*.bin
*.pt
*.gguf

# VPN
config/vpn/

# LibreChat env
docker/.librechat.env

# Activepieces Runtime-Daten
docker/activepieces/

# Blender Binaries
*.blend
*.blend1

# Archives / Exports
*.zip
*.tar.gz
*.xml

# SQLite Datenbanken
*.sqlite

# Pipeline run logs (JSONL, one file per run — runtime data, not source)
pipelines/logs/
# Pipeline run bucket (per-run step outputs for non-linear fan-in — runtime data)
pipelines/bucket/

# Screendesign (Playwright-Screenshots & Design-Input)
apps/pilot/ui/screendesign/

# Personal / not for public repo (decided 2026-05-14)
CLAUDE.md
apps/matrix-tool/
apps/chatterbox-api/
x_briefing/
llm-switch/

# --- Repository boundary: what belongs to a different lifecycle ---
# Host infrastructure, not the mora02 application. The backup system really
# lives in /root/backup-fullsystem.sh, a systemd timer and the Borg repo on the
# NAS; only three convenience wrappers ever sat here. Headed for its own
# private repo together with those parts.
scripts/backup/

# Separate product with its own release cycle, destined for private GitLab.
# Must never land in this public repository.
apps/matrix-tool-v2/
